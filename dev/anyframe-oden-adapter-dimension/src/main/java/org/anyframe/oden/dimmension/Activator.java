package org.anyframe.oden.dimmension;

public class Activator {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		DimensionAdapter adapter = new DimensionAdapter();
		adapter.execute();
		
		/*CheckOut checkOut = new CheckOut();
		checkOut.downLoad(adapter.getServer(), 
				adapter.getUserId(), 
				adapter.getPassword(),
				adapter.getDbName(),
				adapter.getDbConnection(),
				adapter.getProductName(),
				adapter.getRequestId(),
				adapter.getProjectName(),
				adapter.getTargetPath()
				adapter.getR);*/
		
	}

}




